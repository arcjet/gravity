package arcjet:records;

world records {
  record foo {
    float32: f32,
    float64: f64,
    uint32: u32,
    uint64: u64,
    s: string,
    vf32: list<f32>,
    vf64: list<f64>,
  }

  export modify-foo: func(f: foo) -> foo;
  export modify-foo-fallible: func(f: foo) -> result<foo, string>;
}
